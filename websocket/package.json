{
  "name": "websocket",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "prod": "pm2 start ecosystem.config.js --env production",
    "prod-stop": "pm2 stop all",
    "prod-restart": "pm2 restart all",
    "prod-delete": "pm2 delete all",
    "logs": "pm2 logs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.3",
    "uwebsockets.js": "^19.0.0"
  },
  "devDependencies": {
    "pm2": "^5.3.0"
  }
}
